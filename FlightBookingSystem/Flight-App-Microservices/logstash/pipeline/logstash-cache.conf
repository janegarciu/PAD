input {
    tcp {
	    host => "0.0.0.0"
        port => "5001"
		codec => json_lines
    }
}

output {
   elasticsearch {
		hosts => "elasticsearch:9200"
		index => "cache-server-%{+YYYY.MM.dd}"
		user => "elastic"
		password => "changeme"
	}
}
