input {
    tcp {
        port => "5003"
		codec => json_lines
    }
}

output {
    elasticsearch {
		hosts => "elasticsearch:9200"
		index => "flight-server-%{+YYYY.MM.dd}"
		user => "elastic"
		password => "changeme"
	}
}
